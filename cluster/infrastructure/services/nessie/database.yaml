# ==============================================================================
# Nessie PostgreSQL Database (CloudNativePG)
# ==============================================================================

apiVersion: postgresql.cnpg.io/v1
kind: Cluster
metadata:
  name: nessie-db
  namespace: nessie
spec:
  instances: 1

  imageName: ghcr.io/cloudnative-pg/postgresql:16.2

  affinity:
    nodeSelector:
      node.h-kube.io/location: home
    nodeAffinity:
      preferredDuringSchedulingIgnoredDuringExecution:
        - weight: 100
          preference:
            matchExpressions:
              - key: kubernetes.io/hostname
                operator: In
                values:
                  - monkeybusiness

  storage:
    size: 5Gi
    storageClass: local-path

  bootstrap:
    initdb:
      database: nessie
      owner: nessie

  backup:
    barmanObjectStore:
      destinationPath: s3://cnpg-backups/nessie
      endpointURL: http://seaweedfs-s3.seaweedfs.svc.cluster.local:8333
      s3Credentials:
        accessKeyId:
          name: nessie-db-backup-creds
          key: ACCESS_KEY_ID
        secretAccessKey:
          name: nessie-db-backup-creds
          key: SECRET_ACCESS_KEY
    retentionPolicy: "7d"

---
# ==============================================================================
# Scheduled Backup
# ==============================================================================

apiVersion: postgresql.cnpg.io/v1
kind: ScheduledBackup
metadata:
  name: nessie-db-backup
  namespace: nessie
spec:
  schedule: "0 0 * * *"
  backupOwnerReference: self
  cluster:
    name: nessie-db
