# ==============================================================================
# Quant GitRepository
# ==============================================================================
# Source for financial infrastructure - Flux watches this repo for changes
# ==============================================================================

apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: quant
  namespace: flux-system
spec:
  interval: 5m
  url: https://github.com/matta-kelly/quant
  ref:
    branch: main
  # Uncomment if repo is private:
  # secretRef:
  #   name: quant-git-auth
